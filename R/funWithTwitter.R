#' R Package: funWithTwitter
#'
#' This package can get you a tweet's details from a user's timeline or a tweet's ID and embed the tweet into R markdown.
#'
#' The main purpose of this package is to display tweets with IDs either copied from a tweet's URL, for example: "https://twitter.com/STAT545/status/506900131086880768"
#' or acquired using the function `getUserTimeline()` to acquire a number of the most recent tweets in a user's timeline.
#' The IDs can be used to display the tweet as text using `getTweet()` or wrapped in html to embed in Rmarkdown reports using `embedTweet()`.
#'
#' @docType package
#' @name funWithTwitter
NULL
